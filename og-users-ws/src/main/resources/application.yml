server:
  port: ${PORT:0}

spring:
  application:
    name: og-users-ws

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8010/eureka
  instance:
    instance-id: ${spring.application.name}:${random.uuid}